<%= form_for(@status) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Tweet new status..." %>
  </div>
  <%= f.submit "Tweet", class: "btn btn-large btn-primary",style:"float:right;" %>
<% end %>